import 'package:flutter/material.dart';

const paddingGridColoneInterne = EdgeInsets.symmetric(
  vertical: 4,
  horizontal: 12,
);
const paddingGridColoneInterneDashboard = EdgeInsets.symmetric(
  vertical: 2,
  horizontal: 6,
);
const EdgeInsets paddingGridHeader = EdgeInsets.symmetric(
  horizontal: 12.0,
  vertical: 2,
);
const EdgeInsets paddingRowButton = EdgeInsets.symmetric(
  horizontal: 8.0,
);
const EdgeInsets paddingWidgetContainer = EdgeInsets.all(
  12.0,
);
const EdgeInsets paddingGeneral8 = EdgeInsets.all(
  8.0,
);
const EdgeInsets paddingOngletSelection = EdgeInsets.symmetric(
  horizontal: 16.0,
  vertical: 12.0,
);
const EdgeInsets paddingOngletSelectionWithIconVert = EdgeInsets.only(
  left: 16.0,
  right: 16.0,
  bottom: 12.0,
  top: 8,
);
const EdgeInsets paddingOngletToggle = EdgeInsets.symmetric(
  horizontal: 8.0,
  vertical: 4.0,
);
const EdgeInsets paddingVertical8 = EdgeInsets.symmetric(
  vertical: 8.0,
);
const EdgeInsets paddingToolbar = EdgeInsets.symmetric(
  horizontal: 16.0,
  vertical: 8.0,
);

const Radius radius = Radius.circular(
  5.0,
);
const BorderRadius radiusGlobal = BorderRadius.all(
  Radius.circular(
    5.0,
  ),
);
const BorderRadius radiusTopOnly = BorderRadius.only(
  topLeft: radius,
  topRight: radius,
);
const radiusLeftOnly = BorderRadius.only(
  topLeft: radius,
  bottomLeft: radius,
);
const BorderRadius radiusRightOnly = BorderRadius.only(
  topRight: radius,
  bottomRight: radius,
);
